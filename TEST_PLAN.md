# テスト計画: Spring Boot + React 移行 (PR #6)

前提: PostgreSQL は `docker compose down -v && up -d` でシード初期化済み。backend=8080、frontend dev=5173 起動済み。
シード: SKU-1001(128000/24), 1002(2800/6), 1003(4500/3), 1004(15800/40), 1005(46000/8), 1006(9800/15)、注文#1 合計261,600円。
根拠コード: frontend/src/pages/ProductListPage.tsx, ProductFormPage.tsx, OrderListPage.tsx, OrderCreatePage.tsx,
backend ProductRepository.java(search/decreaseStock), InsufficientStockException.java, application.yml(low-stock-threshold=10)。

## T1 商品一覧の表示（金額書式・低在庫バッジ）
1. http://localhost:5173/products を開く。
- 合格: 6行表示。価格が「128,000 円」「46,000 円」のように3桁区切り+円。
- 合格: 「⚠ 在庫少」バッジは SKU-1002(6)/1003(3)/1005(8) の3件のみ。SKU-1006(15)・1001(24)・1004(40) には無い。

## T2 検索（名前/SKU 部分一致・大文字小文字無視）と低在庫フィルタの AND
1. キーワード欄に「マウス」と入力し「検索」→ 合格: SKU-1002 の1行のみ。
2. キーワードを「sku-100」（小文字）に変更し「検索」→ 合格: 6行全件（SKU 大文字小文字無視の部分一致）。
3. その状態で「低在庫のみ表示」をチェック → 合格: SKU-1002/1003/1005 の3行のみ（AND 条件）。
4. チェックを外し、キーワードを「ZZZ該当なし」で検索 → 合格: 「該当する商品はありません。」表示。

## T3 商品登録のバリデーションと成功登録
1. /products/new で全項目空のまま「保存」→ 合格: 4件のフィールドエラー
   「SKUを入力してください。」「商品名を入力してください。」「価格は数値で入力してください。」「在庫数は整数で入力してください。」。画面遷移しない。
2. SKU=SKU-9001、商品名=テスト商品A、価格=-1、在庫数=-5 で「保存」→ 合格: 「価格は0以上で入力してください。」「在庫数は0以上で入力してください。」。遷移しない。
3. 価格=3500、在庫数=12 に直して「保存」→ 合格: /products へ遷移し SKU-9001 テスト商品A 3,500 円 / 12（バッジなし）が表示。

## T4 商品編集
1. SKU-9001 行の「編集」→ 合格: 既存値（SKU-9001 / テスト商品A / 3500 / 12）がフォームに入る。
2. 在庫数を 4 に変更して「保存」→ 合格: 一覧で SKU-9001 の在庫が 4 かつ「⚠ 在庫少」バッジが付く（しきい値10未満）。

## T5 注文一覧（明細と合計）
1. /orders を開く → 合格: 注文#1（株式会社サンプル商事 / SHIPPED）に明細2行
   ノートPC 128,000 円×2=256,000 円、ワイヤレスマウス 2,800 円×2=5,600 円、合計 261,600 円。

## T6 新規注文（クライアント合計・在庫減算）
1. /orders/new で得意先名=テスト商事、SKU-1001 数量=2、SKU-1006 数量=3 を入力
   → 合格: 「合計: 285,400 円」（256,000+29,400）がクライアント側で表示。
2. 「注文を確定」→ 合格: /orders へ遷移し、新規注文が明細2行・合計 285,400 円で表示。
3. /products を開く → 合格: SKU-1001 在庫 24→22、SKU-1006 15→12。

## T7 在庫超過注文（409 エラーと在庫不変）
1. /orders/new で得意先名=超過テスト、SKU-1003（在庫3）数量=5 を入力し「注文を確定」
   → 合格: エラー「「USB-Cハブ (7in1)」の在庫が不足しています(在庫数: 3)。」が表示され、/orders へ遷移しない。
2. /products を開く → 合格: SKU-1003 の在庫は 3 のまま。/orders に超過テストの注文が作られていない。
